# Patron Hub environment configuration

# Optional: override where local SQLite + other app data are stored.
# Default: ./data
PATRON_HUB_DATA_DIR=

# Optional: override where archived files will be stored.
# Default: ./archive
PATRON_HUB_ARCHIVE_DIR=

# Optional: disable auto schema bootstrap + sample seeding (useful for production)
PATRON_HUB_SKIP_BOOTSTRAP=0

# Optional: disable all app auth (LAN/trusted network only). 0 = auth enabled, 1 = no login required.
PATRON_HUB_DISABLE_AUTH=0

# Optional: LAN HTTP port (used by docker-compose.lan.yml). Default: 3000
PATRON_HUB_HTTP_PORT=3000

# Optional: Patreon cookie for sync (recommended to set here, not in the UI, because the value is very long).
# IMPORTANT: Must be the full raw Cookie header value (ASCII only, no “…” truncation).
PATRON_HUB_PATREON_COOKIE=

# Optional: Patreon history depth per sync. Higher = more backlog harvesting, slower sync.
PATRON_HUB_PATREON_MAX_PAGES=40

# Optional: backlog URL-resolution queue controls.
PATRON_HUB_PATREON_HARVEST_JOB_LIMIT=80
PATRON_HUB_PATREON_HARVEST_MAX_ATTEMPTS=8

# Optional: when new assets are discovered (including by headless), how many items to auto-archive per sync.
PATRON_HUB_PATREON_PENDING_ASSET_ARCHIVE_LIMIT=40

# Internal API (used by optional headless worker)
# Generate with: openssl rand -hex 24
PATRON_HUB_INTERNAL_TOKEN=

# Public deployment (Caddy reverse proxy)
PATRON_HUB_DOMAIN=
PATRON_HUB_EMAIL=
PATRON_HUB_BASIC_AUTH_USER=
PATRON_HUB_BASIC_AUTH_HASH=
