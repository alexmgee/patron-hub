{$PATRON_HUB_DOMAIN} {
  tls {$PATRON_HUB_EMAIL}

  encode zstd gzip

  # Defense-in-depth: app auth exists, but keep HTTP basic auth at the edge for public exposure.
  basicauth {
    {$PATRON_HUB_BASIC_AUTH_USER} {$PATRON_HUB_BASIC_AUTH_HASH}
  }

  reverse_proxy patron-hub:3000
}
